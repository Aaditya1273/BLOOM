{
  "name": "@bloom/reporter",
  "version": "1.0.0",
  "private": true,
  "description": "Bloom halt-aware reporter: Robinhood Stock Token API -> signed EIP-712 MarketReport -> Bloom risk engine",
  "type": "module",
  "main": "src/index.ts",
  "engines": { "node": ">=23.6" },
  "scripts": {
    "once": "node src/cli.ts --once",
    "loop": "node src/cli.ts --loop --interval 30",
    "test": "node --test test/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "ethers": "^6.16.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^6.0.3"
  }
}
